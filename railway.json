{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm --workspace @advanced-datatable/mock-backend run build"
  },
  "deploy": {
    "startCommand": "npm --workspace @advanced-datatable/mock-backend run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
